# Library-Management-System
A simple Tkinter app for library book management: add books by title/ID, mark as borrowed/returned with timestamps, remove entries, and view in a table. Run via Python 3.x; demo-only, add database for persistence.
